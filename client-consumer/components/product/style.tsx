import styled from "styled-components";

export const CoreProduct = styled.section.attrs({
  className: "flex flex-col md:flex-row gap-12",
})``;

export const GalleryContent = styled.section.attrs({
  className: "w-full md:w-[55%]",
})``;

export const LayoutSide = styled.section.attrs({
  className: "w-full md:w-[45%]",
})``;
